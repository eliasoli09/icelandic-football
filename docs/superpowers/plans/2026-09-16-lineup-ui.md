# Lineup UI implementation plan

**Goal:** Upgrade the existing Byrjunarliðið game after showing a visual concept.
**Architecture:** Keep XiGame state and all game functions unchanged. Restructure its JSX into a page introduction, match header, pitch panel and sidebar; scope all styling to Xi.module.css. Preserve the existing global navigation.
**Stack:** Next.js 16, React 19, CSS Modules, Lucide icons.

- [x] Update `web/src/components/Xi/XiGame.tsx` with Icelandic title, segmented difficulty controls, match header, pitch markings, live progress, instructions and archive panel. Keep score guesses, team switching, difficulty selection, saved state, sharing and player dialogs connected to their existing handlers.
- [x] Restyle `web/src/components/Xi/Xi.module.css` with charcoal/sage/gold theme-aware surfaces, a forest-green pitch, readable player chips, restrained borders, visible keyboard focus and reduced-motion support. Use a two-column desktop grid and one-column mobile layout; retain minimum 44px controls.
- [x] Run existing XI tests and production build. Inspect actual browser output on desktop and narrow mobile; exercise team/difficulty selection, score and player guesses, archive selection, persistence, give-up confirmation and dialog keyboard input. No extra tests duplicating CSS or markup.
- [x] Review diff, publish the verified update to the existing linked Vercel project if deployment access is available, and verify the live page.

## Visual design
Charcoal and warm gold, using the site's existing sans-serif typography. Large editorial heading, compact match score strip, a wide five-row tactical pitch and a smaller progress/results/instructions column. Match sources remain accessible in a disclosure below the game. The image is a direction preview; real data, labels and formations remain authoritative. Light mode uses the site's existing surface tokens, keeping the pitch dark for contrast.

## Validation
- 100 XI tests passed across xi.test.ts and xi-parse.test.ts.
- Production build and TypeScript checks passed.
- Browser: desktop, 390px and 320px mobile, dark/light themes; no horizontal overflow at mobile widths.
- Verified correct player guess, score submission, saved state after reload, team/difficulty switching, archive selection and give-up cancellation; longest-name dialog fits at 320px.
- Browser console returned no warnings or errors.
- Published to https://islensk-fotbolti.vercel.app/byrjunarlid and verified the new heading on the live page. Vercel deployment: dpl_HuRgYeA9XfcMXuGCXzGCbpazAdKC.
